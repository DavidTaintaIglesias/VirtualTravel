version: '3'

services:

  database:
    container_name: ejercicioFinal
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: VirtualTravel
      MYSQL_USER: user
      MYSQL_PASSWORD: password
      MYSQL_ROOT_PASSWORD: password
    ports:
      - '3306:3306'